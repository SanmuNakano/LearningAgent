{
  "name": "openclaw-plugin-qq-study-router",
  "version": "0.5.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node ./scripts/clean.mjs && tsc -p tsconfig.build.json",
    "check": "npm run build && npm test",
    "plugin:validate": "npm run release:check",
    "plugin:inspect": "npm run build && openclaw plugins inspect qq-study-router --json",
    "plugin:doctor": "npm run build && openclaw plugins doctor",
    "prepack": "npm run build",
    "release:check": "npm run check && node ./scripts/release-check.mjs && npm pack --dry-run",
    "test": "vitest run",
    "supervisor:serve": "npm run build && node ./dist/supervisor.js --serve --project D:\\learn\\openclaw-plugins",
    "supervisor:worker:inbox": "npm run build && node ./dist/supervisor.js --worker-inbox --project D:\\learn\\openclaw-plugins",
    "supervisor:worker:heartbeat": "npm run build && node ./dist/supervisor.js --worker-heartbeat working --project D:\\learn\\openclaw-plugins",
    "supervisor:worker:codex": "npm run build && node ./dist/supervisor.js --worker-codex --project D:\\learn\\openclaw-plugins --worker-id codex-main",
    "supervisor:notification:outbox": "npm run build && node ./dist/supervisor.js --notification-outbox --project D:\\learn\\openclaw-plugins"
  },
  "files": [
    "dist",
    "openclaw.plugin.json",
    "README.md",
    "CHANGELOG.md",
    "docs/project-supervisor/MIGRATION_0.5.0.md"
  ],
  "peerDependencies": {
    "openclaw": ">=2026.5.17"
  },
  "dependencies": {
    "typebox": "^1.1.38"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "openclaw": "latest",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  }
}
